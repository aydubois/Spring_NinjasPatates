spring:
  mvc:
    hiddenmethod:
      filter:
        enabled: true
  jpa:
    show-sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/java
    username: root
    password: '{SQL}#42sqlpass'
server:
  port: 8082
